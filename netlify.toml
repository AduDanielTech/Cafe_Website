[functions]
external_node_modules = ["express"]
node_bundler = "esbuild"
[[redirects]]
force = true
from = "/api/*"
status = 200
to = "/.netlify/functions/api/:splat"
[dev]
command = "npm run dev"
port = 3001  # Change the port to a different value, e.g., 3001
